type Query {
    manufacturers(manufacturerInput : ManufacturerInput)      : [Manufacturer]
    series(seriesInput              : SeriesInput)            : [Series]
    models(modelInput               : ModelInput
           priceInput               : NumericComparisonInput) : [Model]
}